- Loader 默认采用静默失败策略，仅在捕获异常时通过 `emitWarning` 输出警告，确保构建流程不因单个文件解析错误而中断。
- 支持通过 `DEBUG=webmcp` 环境变量开启详细的控制台日志，用于追踪文件处理状态和转换结果。
- Plugin 配置中的相对路径（如 `include`）会在注入 Webpack 规则前，基于 `compiler.context` 自动转换为绝对路径以符合 RuleSet 要求。