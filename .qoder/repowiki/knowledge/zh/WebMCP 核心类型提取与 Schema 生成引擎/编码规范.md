- 采用“快速失败”策略，在 `transformCode` 和 `extractToolsFromFile` 入口处通过关键词预检，避免对无关文件启动重型 AST 解析。
- 统一通过 JSDoc 标签（如 `@readonly`）和行内注释提取业务元数据，实现代码逻辑与配置信息的解耦。
- 在类型映射中实施深度限制（depth < 3）和默认降级策略，将复杂或未知的 TypeScript 类型统一收敛为 `string` 以保证 Schema 生成的稳定性。