- 所有业务 Store 均遵循“Provider + Hook”模式，通过 `createContext` 创建上下文，并导出对应的 `useXxxStore` 钩子以获取状态与方法。
- Store 中的异步或同步操作方法统一接收对象类型的 Input 参数（如 `CreateTodoInput`），而非分散的参数列表，以保持接口一致性。
- UI 组件与页面文件采用 PascalCase 命名（如 `TodosPage.tsx`, `RichTextEditor.tsx`），而工具函数模块采用 kebab-case 命名（如 `navigation-bridge.ts`）。