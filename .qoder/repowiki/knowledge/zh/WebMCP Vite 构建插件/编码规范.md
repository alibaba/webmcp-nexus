- 插件选项采用默认值策略，未提供时使用内置的 include 模式与空 alias 映射。
- 在 transform 阶段先做文件扩展名与 include 匹配的快速短路，减少不必要的处理开销。
- 对核心转换调用使用 try/catch 包裹，失败时通过 this.warn 输出警告并返回 null，避免阻断构建。
- alias 归一化仅保留 string 类型的 find/replacement 项，忽略 RegExp 形式的 alias。