- 采用 pnpm workspace 管理 `apps/*` 与 `packages/*`，通过 `pnpm -r` 实现跨模块依赖解析与并行构建。
- 根目录定义统一的 `tsconfig.base.json` 与 `eslint.config.js`，强制所有子模块遵循一致的 TypeScript 编译选项（如 `verbatimModuleSyntax`）与代码规范。
- 顶层 `package.json` 封装了从本地开发 (`dev`)、多环境构建 (`build:webpack`) 到一键发布 (`release`) 的全生命周期脚本，屏蔽底层子模块的调用细节。