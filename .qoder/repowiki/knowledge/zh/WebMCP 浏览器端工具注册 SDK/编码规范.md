- 所有公共 API 均包含多层级的 try-catch 兜底机制，确保在浏览器环境异常或 API 缺失时不会导致宿主应用崩溃。
- 采用“所有权注册表”模式管理同名工具，通过 scopeId 追踪多个组件对同一工具的引用，仅在最后一个持有者卸载时才执行原生注销。
- 利用 `AbortController` 信号与原生 `registerTool` 的 signal 选项对接，实现工具随组件卸载自动清理，减少手动维护负担。